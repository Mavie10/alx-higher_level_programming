#!/usr/bin/python3
"""module object is an instance of, or if the object is an instance"""


def is_kind_of_class(obj, a_class):
    """if an object is a subclass of a class or no """
    return isinstance(obj, a_class)
